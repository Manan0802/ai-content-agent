name = "aica-review"
pages_build_output_dir = "public"
compatibility_date = "2026-07-01"

# Created with:  npx wrangler d1 create aica-review
# Paste the printed database_id below, then:  npx wrangler d1 execute aica-review --remote --file schema.sql
[[d1_databases]]
binding = "DB"
database_name = "aica-review"
database_id = "PASTE_AFTER_d1_create"
